<h1> 購物車</h1>
<%= link_to '清空', cart_path, method: :delete %>

<%= render 'cart', cart: current_cart %>

<%= link_to "繼續選購", pricing_path %>
<%= link_to "結帳", checkout_cart_path unless current_cart.empty? %>